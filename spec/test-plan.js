/** Test components */
//Available options :: TestUtils, Enzyme, Jest

//Test cases
it('renders Repo component', function() {
  //Set props, assert the structure of the DOM returned
});

it('renders RepoList component', function() {
 //Set props, assert the structure of the DOM returned
});

describe('FilterBy' function() {
  it('renders FilterBy component', function() {
    //Set props, assert the structure of the DOM returned
  });

  it('calls endpoint posts with params', function() {
    //Simulate Click on a filter, assert the API query made
  });

  it('renders filtered repos on click', function() {
    //Simulate Click on a filter, assert the DOM returned
  });
});

it('renders Button component', function() {
 //assert the structure of the DOM returned
});

describe('Paginator' function() {
  it('renders Paginator component', function() {
    //Set props, assert the structure of the DOM returned
  });

  it('calls endpoint posts with params', function() {
    //Simulate Click on a page, assert API query made
  });

  it('sets the active class on click', function() {
    //Simulate Click on a page, assert the class on the active
  });

  it('shows repos from prev page on click', function() {
    //Simulate Click on a page, assert the class on the active
  });

  it('shows repos from next page on click', function() {
    //Simulate Click on a page, assert the class on the active
  });

  it('calls endpoint posts with params on click prev', function() {
    //Simulate Click on a page, assert API query made
  });

  it('calls endpoint posts with params on click prev', function() {
    //Simulate Click on a page, assert API query made
  });
});

describe('FilterableReposList' function() {
  it('renders FilterableReposList component', function() {
    //Set mock data, assert the structure of the DOM returned
  });

  it('calls endpoint posts and filters with params', function() {
    //Render the component, assert API query made
  });

  it('onPageSelection()', function() {

  });

  it('onFilterSelection', function() {

  });

  it('setPages', function() {

  });
});